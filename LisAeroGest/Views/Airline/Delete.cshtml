@using LisAeroGest.Data.Interfaces
@model LisAeroGest.Data.Entities.Airline
@inject IImageHelper ImageHelper

@{
    ViewData["Title"] = "Eliminar Companhia Aérea";
}


<nav aria-label="breadcrumb" class="mt-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Início</a></li>
        <li class="breadcrumb-item"><a asp-controller="Airline" asp-action="Index">Companhias</a></li>
        <li class="breadcrumb-item active">Eliminar</li>
    </ol>
</nav>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="d-flex align-items-center mb-4">
                <a asp-action="Index" class="btn btn-outline-secondary btn-sm me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <h2 class="fw-bold mb-0 text-danger">
                    <i class="bi bi-trash-fill me-2"></i>Eliminar Companhia Aérea
                </h2>
            </div>

            <div class="card shadow-sm border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>Confirmar Eliminação
                    </h4>
                </div>

                <div class="card-body p-4">

                 
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>

                    <div class="alert alert-danger d-flex align-items-center gap-2 mb-4">
                        <i class="bi bi-exclamation-triangle-fill"></i>
                        Esta operação não pode ser revertida.
                    </div>

                    <div class="text-center mb-4">
                        <img src="@ImageHelper.GetImageUrl(Model.ImageId, "airlines")"
                             alt="@Model.Name"
                             class="rounded shadow-sm"
                             style="height:100px; object-fit:cover;" />
                    </div>

                    <table class="table table-borderless">
                        <tr>
                            <th class="text-muted" style="width:35%">Nome</th>
                            <td class="fw-semibold">@Model.Name</td>
                        </tr>
                        <tr>
                            <th class="text-muted">Código IATA</th>
                            <td>
                                <span class="badge bg-primary">@Model.IATACode</span>
                            </td>
                        </tr>
                        <tr>
                            <th class="text-muted">País</th>
                            <td>@Model.Country</td>
                        </tr>
                    </table>

                    <form id="deleteForm" asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex gap-2 mt-3">
                            <button type="submit" id="btnSubmit" class="btn btn-danger">
                                <i class="bi bi-trash me-2"></i>Confirmar Eliminação
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        document.getElementById('deleteForm').addEventListener('submit', function () {
            var btn = document.getElementById('btnSubmit');
            btn.disabled = true;
            btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>A eliminar...';
        });
    </script>
}